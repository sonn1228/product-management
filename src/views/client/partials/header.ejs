<style>
  .nav {
    padding: 10px;
  }

  .header {
    width: 100%;
    position: sticky;
    top: 0;
    z-index: 999;
  }
</style>
<div class="header">
  <% function subMenu(records, pathProduct) { %>
    <ul class="ml-4 space-y-2">
      <% records.forEach(item=> { %>
        <li>
          <a href="<%= pathProduct %>/<%= item.slug %>" class="block px-4 py-2 text-gray-700 hover:bg-gray-200 rounded">
            <%= item.title %>
          </a>
          <% if (item.children && item.children.length> 0) { %>
            <%- subMenu(item.children, pathProduct) %>
              <% } %>
        </li>
        <% }); %>
    </ul>
    <% } %>

      <nav class="bg-yellow-500  w-full z-10 nav">
        <div class="container mx-auto px-4">
          <div class="flex justify-between items-center py-2">
            <!-- Logo -->
            <a href="/" class="flex items-center">
              <img src="https://rubee.com.vn/admin/webroot/upload/image//images/logo-the-gioi-di-dong-2.jpg"
                alt="<%= settingsGeneral.websiteName %>" class="h-8 mr-2">
              <span class="text-xl font-semibold text-white">
                <%= settingsGeneral.websiteName %>
              </span>
            </a>

            <!-- Search Bar -->
            <form action="/search" method="GET" class="flex items-center w-1/3">
              <input type="search" name="keyword" placeholder="Bạn tìm gì..."
                class="w-full px-4 py-2 rounded-l focus:outline-none">
              <button type="submit" class="px-4 py-2 bg-gray-200 text-gray-800 rounded-r">🔍</button>
            </form>

            <!-- Navigation Links -->
            <div class="flex items-center space-x-4 text-white">
              <a href="/user/login" class="flex items-center hover:underline">
                <span>Đăng nhập</span>
              </a>
              <a href="/user/register" class="flex items-center hover:underline">
                <span>Đăng ký</span>
              </a>

              <a href="/cart" class="flex items-center hover:underline">
                🛒 <span>Giỏ hàng (<%= miniCart ? miniCart.products.length : 0 %>)</span>
              </a>
              <div class="flex items-center">
                <span>📍 Hồ Chí Minh</span>
                <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                </svg>
              </div>
            </div>
          </div>


          <!-- Product Categories -->
          <div class="flex justify-center space-x-6 mt-2">
            <% productCategory.forEach(item=> { %>
              <a href="/<%= item.slug %>" class="flex flex-col items-center hover:underline">
                <div class="flex items-center gap-1">
                  <img src="<%= item.thumbnail %>" alt="<%= item.title %>" class="w-7 h-7 rounded-full object-cover">
                  <div class="text-sm text-gray-700">
                    <%= item.title %>
                  </div>
                </div>
              </a>
              <% }); %>
          </div>
        </div>
      </nav>

      <!-- Nội dung bên dưới -->
      <div class="pt-20">
        <!-- Nội dung của trang nằm ở đây -->
      </div>
</div>